# Backend base URL. Defaults to the same-origin "/api/v1", which the Vite dev
# server proxies to http://localhost:8080 (see vite.config.ts). Override only if
# the backend lives elsewhere — then cookies must be cross-origin friendly.
VITE_API_URL=/api/v1
